#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <pthread.h>

int main(int argc, char* argv[])
{
    // Check invalid inputs
    if (argc != 4) {

    }

    // Initiate thread and etc


    // Run find function


    // Finish program (free, ...)

    return 0;
}